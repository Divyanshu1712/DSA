let valueStr = 'hello';
// output should be 'olleh'

function revsresStr(valueStr) {
    let resStr = undefined;
    let len = valueStr.length;
    console.log(len);
    for (let i = len; i <= 1; i--) {
        let check = i;
        console.log(check);

    }



};

const finalStr = revsresStr(valueStr)
console.log(finalStr);
